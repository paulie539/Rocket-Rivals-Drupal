{#
/**
 * @file
 * Renders the main menu block without a wrapper div.
 *
 * The menu itself is rendered by menu--main.html.twig via {{ content }}.
 * We skip the standard block wrapper so the menu output (.hero-nav)
 * positions correctly within the hero overlay.
 */
#}
{{ content }}
